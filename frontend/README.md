# Interface do Sistema de Gestão de Escalas

Aplicação React com TypeScript responsável pela interface do sistema portuário.

## Execução local

```powershell
npm.cmd install
npm.cmd run dev
```

A variável `VITE_API_URL` define o endereço da API. Na ausência dela, a interface utiliza `http://localhost:8080`.

